// predict.js

function predict() {
    const race = document.getElementById("race").value;
    const drugType = document.getElementById("drug_type").value;
    const year = document.getElementById("year").value;

    // Placeholder logic
    const prediction = "Predicted sentence: Less than 36 months";  // Static message

    document.getElementById("result").innerText = prediction;
}
